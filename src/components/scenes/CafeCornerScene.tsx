/**
 * CafeCornerScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function CafeCornerScene).
 */
import { CafeCornerScene } from '../LocationScene';

export default CafeCornerScene;
