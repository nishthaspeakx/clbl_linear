/**
 * ParkBenchScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function ParkScene).
 */
import { ParkScene } from '../LocationScene';

export default ParkScene;
