/**
 * RestaurantMenuScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function RestaurantMenuScene).
 */
import { RestaurantMenuScene } from '../LocationScene';

export default RestaurantMenuScene;
