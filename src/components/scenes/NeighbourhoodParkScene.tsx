/**
 * NeighbourhoodParkScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function NeighbourhoodParkScene).
 */
import { NeighbourhoodParkScene } from '../LocationScene';

export default NeighbourhoodParkScene;
