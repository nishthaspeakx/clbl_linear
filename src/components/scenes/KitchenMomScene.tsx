/**
 * KitchenMomScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function KitchenScene).
 */
import { KitchenScene } from '../LocationScene';

export default KitchenScene;
