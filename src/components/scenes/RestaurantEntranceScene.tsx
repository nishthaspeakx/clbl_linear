/**
 * RestaurantEntranceScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function RestaurantEntranceScene).
 */
import { RestaurantEntranceScene } from '../LocationScene';

export default RestaurantEntranceScene;
