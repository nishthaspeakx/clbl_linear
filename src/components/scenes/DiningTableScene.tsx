/**
 * DiningTableScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function DiningTableScene).
 */
import { DiningTableScene } from '../LocationScene';

export default DiningTableScene;
