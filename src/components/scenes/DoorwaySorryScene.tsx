/**
 * DoorwaySorryScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function DoorwaySorryScene).
 */
import { DoorwaySorryScene } from '../LocationScene';

export default DoorwaySorryScene;
