/**
 * BusStopScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function BusStopScene).
 */
import { BusStopScene } from '../LocationScene';

export default BusStopScene;
