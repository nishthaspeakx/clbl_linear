/**
 * BalconyHelpScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function BalconyScene).
 */
import { BalconyScene } from '../LocationScene';

export default BalconyScene;
