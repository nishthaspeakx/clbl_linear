/**
 * RestaurantHostDeskScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function RestaurantHostScene).
 */
import { RestaurantHostScene } from '../LocationScene';

export default RestaurantHostScene;
