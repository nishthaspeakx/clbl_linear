/**
 * HomeRoutineScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function HomeRoutineScene).
 */
import { HomeRoutineScene } from '../LocationScene';

export default HomeRoutineScene;
