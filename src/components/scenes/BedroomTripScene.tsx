/**
 * BedroomTripScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function BedroomTripScene).
 */
import { BedroomTripScene } from '../LocationScene';

export default BedroomTripScene;
