/**
 * RestaurantOrderingScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function RestaurantOrderingScene).
 */
import { RestaurantOrderingScene } from '../LocationScene';

export default RestaurantOrderingScene;
