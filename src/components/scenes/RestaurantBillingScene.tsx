/**
 * RestaurantBillingScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function RestaurantBillingScene).
 */
import { RestaurantBillingScene } from '../LocationScene';

export default RestaurantBillingScene;
