/**
 * TownGateScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function TownGateScene).
 */
import { TownGateScene } from '../LocationScene';

export default TownGateScene;
