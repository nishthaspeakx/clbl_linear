/**
 * LivingRoomFatherScene — rich isometric scene (reuses the shared iso toolkit).
 * Edit the implementation in ../LocationScene (function LivingRoomScene).
 */
import { LivingRoomScene } from '../LocationScene';

export default LivingRoomScene;
