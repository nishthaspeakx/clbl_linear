/**
 * HomeScreen — the English Town map presented as the app's Home tab.
 * The map screen owns its own SpeakX-style header + floating topic card.
 */
import React from 'react';
import EnglishTownScreen from './EnglishTownScreen';

export default function HomeScreen() {
  return <EnglishTownScreen />;
}
